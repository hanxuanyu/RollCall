name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            goos: windows
            goarch: amd64
          - os: macos-latest
            goos: darwin
            goarch: amd64
          - os: ubuntu-latest
            goos: linux
            goarch: amd64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Determine version
        id: version
        shell: bash
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          COMMIT_ID=$(git rev-parse --short HEAD)
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "commit=${COMMIT_ID}" >> "$GITHUB_OUTPUT"
          echo "ldflags=-X RollCall/internal/version.Version=${TAG} -X RollCall/internal/version.CommitID=${COMMIT_ID}" >> "$GITHUB_OUTPUT"

      - name: Install Wails
        shell: bash
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      # --- Desktop build ---
      - name: Build desktop (Windows)
        if: matrix.goos == 'windows'
        shell: bash
        run: wails build -ldflags "${{ steps.version.outputs.ldflags }}"

      - name: Build desktop (macOS)
        if: matrix.goos == 'darwin'
        shell: bash
        run: wails build -ldflags "${{ steps.version.outputs.ldflags }}"

      - name: Build desktop (Linux)
        if: matrix.goos == 'linux'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.0-dev
          wails build -ldflags "${{ steps.version.outputs.ldflags }}"

      # --- Server build ---
      - name: Build server
        shell: bash
        run: |
          EXT=""
          if [ "${{ matrix.goos }}" = "windows" ]; then EXT=".exe"; fi
          CGO_ENABLED=0 GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} go build \
            -tags server \
            -ldflags "${{ steps.version.outputs.ldflags }}" \
            -o "build/bin/RollCallServer${EXT}" .

      # --- Package desktop ---
      - name: Package desktop (Windows)
        if: matrix.goos == 'windows'
        shell: bash
        run: |
          cd build/bin
          7z a ../../RollCall-desktop-windows.zip RollCall.exe

      - name: Package desktop (macOS)
        if: matrix.goos == 'darwin'
        shell: bash
        run: |
          brew install create-dmg || true
          create-dmg \
            --volname "RollCall" \
            --no-internet-enable \
            "RollCall-desktop-macos.dmg" \
            "build/bin/RollCall.app" || true
          if [ ! -f RollCall-desktop-macos.dmg ]; then
            cd build/bin && tar czf ../../RollCall-desktop-macos.tar.gz RollCall.app
          fi

      - name: Package desktop (Linux)
        if: matrix.goos == 'linux'
        shell: bash
        run: |
          chmod +x build/bin/RollCall
          cd build/bin && tar czf ../../RollCall-desktop-linux.tar.gz RollCall

      # --- Package server ---
      - name: Package server (Windows)
        if: matrix.goos == 'windows'
        shell: bash
        run: |
          cd build/bin
          7z a ../../RollCall-server-windows.zip RollCallServer.exe

      - name: Package server (macOS)
        if: matrix.goos == 'darwin'
        shell: bash
        run: |
          chmod +x build/bin/RollCallServer
          cd build/bin && tar czf ../../RollCall-server-macos.tar.gz RollCallServer

      - name: Package server (Linux)
        if: matrix.goos == 'linux'
        shell: bash
        run: |
          chmod +x build/bin/RollCallServer
          cd build/bin && tar czf ../../RollCall-server-linux.tar.gz RollCallServer

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.goos }}
          path: |
            RollCall-*.zip
            RollCall-*.tar.gz
            RollCall-*.dmg

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            RollCall-*.zip
            RollCall-*.tar.gz
            RollCall-*.dmg
          generate_release_notes: true
